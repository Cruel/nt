# Preview and Test Playback

## Purpose

Define the editor-facing runtime preview and test playback workflow for the Electron/TanStack/Vite editor direction.

## Current Status

Backend-neutral preview APIs exist for starting, stopping, resetting, stepping, overriding entrypoints, injecting runtime input, inspecting state, and capturing controller commands. Remaining work should harden real editor workflows and project fixtures.
