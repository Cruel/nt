# JSON boundary policy exceptions. Format:
# path|exact matched construct|owning subsystem or target|boundary category|rationale|removal condition
#
# The current tree needs no exceptions: all JSON-visible shipped headers are in the explicit,
# structurally approved codec and adapter paths in CheckJsonBoundaryPolicy.cmake. Add an entry only
# when a legitimate external boundary cannot live in one of those paths.
