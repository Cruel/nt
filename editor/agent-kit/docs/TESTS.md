# Tests

Test records are tracked authoring records under `records/tests/`. Use the installed CLI's test commands for headless/runtime-backed execution. Treat test failures and `noveltea validate` diagnostics as authoritative project feedback; generated `.noveltea/build` output is disposable and must not be tracked.
