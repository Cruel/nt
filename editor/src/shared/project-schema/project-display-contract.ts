/**
 * Maximum authored and compiled reference-resolution dimension supported by
 * the renderer, preview transport, package manifest, and player bootstrap.
 */
export const MAX_REFERENCE_RESOLUTION_DIMENSION = 10_000;
